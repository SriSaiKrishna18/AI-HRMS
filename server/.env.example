# RizeOS — Environment Configuration
# Copy this to .env and fill in your values

# JWT Secret — used for authentication tokens (REQUIRED)
JWT_SECRET=your_jwt_secret_here_change_this

# Server port (default: 5000)
PORT=5000

# Node environment
NODE_ENV=development

# Smart Contract Address (optional — deployed on Sepolia)
# CONTRACT_ADDRESS=0x...
